// print fibonaki series

#include <stdio.h>

int main()
{
    int x, y;
    int i; // for running loop
    int p, q=0; // for calculation purpose

    printf(" Enter till which number you want the fibonaki series  : ");
    scanf(" %d", &y);

    for( x = 1 ; x<= y ; x++)
    {
        q = q + x ;
        if(q > 3)
        {
            p;
        }
        printf(" %d", q);
        
    }
}
