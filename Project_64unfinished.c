// program to eveluate series

#include <stdio.h>

int main()
{
    int a, b; // for taking input for repetation cube, square anything
    int i; // for running loop
    int c=0;

    printf("Enter Number : ");
    scanf (" %d",&b);
    printf(" Enter the value for you want repetation for");
    scanf (" %d", &a);

    for(i=a ; i<=a ; i++)
    {
        
    }
}
